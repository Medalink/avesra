# Plan 001 execution review - 2026-09-25

The owner reaffirmed that execution must continue through the whole plan. Latest
source checkpoint: [qualification, planner and diagnostic integration](../docs/evidence/qualification-diagnostics-2026-09-25.md), with matching Windows static/release and
Spark ARM static/release passes. These artifacts are retained separately, not yet
installed. Reviewed schema13 facts/routines also pass Windows Static after a
checked SQL timestamp conversion. Durable qualification, actual semantic app and
browser workflows, packaging and remaining release requirements are still being
implemented. The [release ledger](../docs/evidence/release.md) remains open;
no section19 checkbox is closed by compilation.

Latest scoped runtime checkpoint: [streaming default/custom preview proof](../docs/evidence/streaming-preview-2026-09-25.md).
Both actual native UI requests completed on matching deployed binaries, with first
submitted speech at 397.0/407.6ms after correcting the measured 6.82s collector
delay. Genuine screenshots, silent native mixer WAVs and synchronized sampled
videos are retained. This does not close C1/C2/C3/C5, OW1-OW3 or Plan001.

## Later publication and background proof

The isolated checkpoint described below was subsequently published to `main`
through `1704d2b`. Follow-up work is in the primary checkout and remains dirty.
The later owner authorization permits isolated, silent background media capture;
it does not permit active-desktop input or owner microphone capture. See the
[runbook](../docs/background-evidence.md) and
[actual native preview evidence](../docs/evidence/background-preview-2026-09-25.md).
Those default/custom recordings are scoped live proof, not completion of Plan001.

The subsequent C1 activity snapshot `D754E1B4F3C957C716468DFA02D39527F5E949A0502613D8DDCDB24C72019E43`
passed Windows formatting/locked Clippy, extension TypeScript and Svelte checks
(zero errors/warnings), then full release in 4m00s. A missing browser-extension
dependency junction initially prevented its frontend check; restoring the existing
locked dependency path resolved it without changing dependency versions. ARM
static/release passed in 5.47s/34.38s. Windows executable SHA256 is
`C93ACDCC736B5A9BE7EE855A2AE1CE34DBA58B9F2EC67C396340F932D927C9E3`;
ARM server SHA256 is `e700c152a723cf311f1bcd8dbbb5d2b03a8e1f009e5d69ca60983a0a413c6627`.
These artifacts were saved separately from the canonical preview binary.
[Activity documentation](../docs/voice-activity.md#isolated-streaming-observation-2026-09-25)
records actual isolated GPU streaming, partial-tail, cancellation and deadline
observations. They do not prove native microphone streaming or owner quality.

The C3 accepted app/volume coordinator, protected permissions, durable projection
and exact cancellation passed independent source review. Review corrected original
session binding, owner-lock timing, caller withdrawal before enqueue, independent
cancellation and stale snapshot ordering. Integrated checks corrected two formatting
issues and a native `Result` error type. Windows static/frontend then passed
(7.51s Rust, zero Svelte errors/warnings); ARM static/release passed in 3.86s/35.18s.
Windows release subsequently passed in 4m01s from corrected source archive
`DE8F2B8C7A678C755DF37FDB63A625BF863151304F2CD62F8DE599204B0CC54F`.
No grants, PC effects, accepted
turns or qualified action workflows were exercised. These observations do not
complete C3 or typed model proposals/owner clarification.

The remaining sections are the earlier isolated execution record. Their baseline
source gaps, publication state and runtime restrictions are historical, not a
description of the later dirty source or explicitly authorized preview runs.

The next native activity-calibration snapshot
`4E7D2DAE6FB114C63DA509EBCB128486061E18AB6D8725327B4535E8090DDB05`
passed independent source review, Windows static/frontend checks (6.95s Rust,
zero Svelte errors/warnings) and release (3m00s). Its saved executable SHA256 is
`1478CE8A2505306A0A69A195D6E6EC49FB4B622A7D6AD6BADFDA0B2A5851C406`.
This implements bounded native frame labels, measured-only provisional policies,
held-out accounting and diagnostic boundaries. No operator labels, microphones,
held-out quality or automatic qualification were exercised. The snapshot is a
source/build checkpoint, not owner-voice acceptance.

## Baseline and ownership

The owner requested `/improve execute 01`, reaffirmed that voice effects and background music now accompany speech, and then explicitly requested committing the current work before continuing. The advisor committed all eleven existing changed/untracked files as `de2e8c8` (`Preserve saved voice recovery and refresh completion plan`). The primary checkout `E:\Dev\Avesra` on `main` was clean immediately afterward. No push was requested or performed.

Execution is isolated in `E:\Dev\Avesra-plan-001-completion`, branch `codex/plan-001-completion`, created from that exact commit. Enrollment recovery, UI changes and the refreshed completion plan are therefore included. The owner subsequently authorized up to five subagents; the host supports three alongside the advisor. Source ownership is split between native browser/C1, browser extension, and C2 speech executors; the advisor owns review and plan status. No changes to the user's protected data, active desktop, microphone or playback are part of this execution. The no-automated-tests and no-Computer-Use constraints remain in force.

The existing shared CPAL voice effects/atmosphere path is a preservation requirement, now explicit in Plan 001. Runtime confirmation of the recovered normal-user physical voice store is historical evidence from the handoff; it was not repeated here.

## Independently observed baseline checks

- `pnpm install --frozen-lockfile --offline` in the isolated worktree: PASS, reused the locked packages without lockfile changes.
- `pnpm -r check`: PASS; extension TypeScript and Svelte, zero errors and zero warnings.
- Windows `pwsh -NoProfile -File scripts/verify.ps1 -Suite Static`: PASS, exit 0, against an immutable `git archive de2e8c8` under `artifacts/execution/baseline-de2e8c8`. Workspace formatting, locked Clippy with warnings denied and recursive frontend checks all passed. The initial cold dependency compilation was deliberately interrupted to release the source-edit hold, then resumed against this frozen archive with one Cargo job and BelowNormal process priority; its completed Clippy run reported 1m44s after dependency warmup. No tests ran. Final changed-source checks are recorded below.
- Actual Spark2 SSH identity: `spark-c8bb`, `aarch64`. No inference or service lifecycle operation was invoked.
- Immutable `de2e8c8` source was archived into `/home/medalink/.local/share/avesra-build/plan001-baseline-de2e8c8`. With source mounted read-only, no GPU, two CPUs and 8 GiB, pinned compiler image `rust@sha256:8fa55b2f3ddf97471ab6a767bfa3f37e6bad0986ba823e75fea57e2a2a5c3073` ran `bash scripts/verify.sh static` and `bash scripts/verify.sh build`: both PASS, exit 0. Reported workspace checking took 5.37 seconds and release compilation 33.16 seconds. Full log: `verification.log` in that directory. This covers contracts/core/server only.

## Historical initial execution boundaries

- C1 source still has no native qualification owner or measured/calibrated signal, overlap, echo and directed-intent producer. Saved enrollment must not manufacture `QualifiedProfile`, `voice_ready` or an accepted turn. Live positive/negative qualification remains pending.
- C2 startup still has `reasoning: None`; the loaded-artifact, immutable request-route, context-capacity and backend-terminal qualification constructor is missing. Read-only inspection of installed Local Studio revision `d1abdef` confirmed its instance record does not include a loaded artifact digest and its local proxy uses dynamic inference routing. Existing local changes to `local-fetch.ts` and `active-model.ts` and untracked patch files were preserved. No credential values were read or printed.
- C4 preparation/finalization foundations had no complete publication/content/settlement loop or extension extraction/outbox activation at baseline. The integrated source now connects these pieces under existing accepted-task authority. No accepted producer or concrete C5 content consumer exists; absent consumers return Unsupported before preparation/publication. Live browser access is deferred.
- OW1-OW3, full owner recognition, long-response speech, later learning/diagnostic/profile work and release qualification remain incomplete. Baseline checks are not product acceptance.

## Implemented source and review

- `3347a48`: C2 step 4 prevalidates the full accepted reply and splits it at UTF-8/word boundaries into at most 64 segments of at most 512 bytes each, preserving exact concatenation. Sequential private TTS jobs retain the original authority, output queue, 720,000-sample/30-second audio cap and original synthesis deadline. Only actual segment completion permits the next job; truncation stops the remainder. Speech protocol version 2 rejects old peers. This is bounded segmentation, not arbitrary-duration speech or qualified reasoning activation. The native renderer and its effects/music path are unchanged.
- `4aa1fa1`: C1 distinguishes directly qualified requests from invitation-bound follow-ups without a mandatory name prefix. It retains qualified profile, signal, overlap, echo and directness gates. No calibrated native producer or automatic voice activation was invented.
- `4aa1fa1`: C4 wires the same-worker browser read claim, opaque publication/content endpoints, synchronous borrowed content consumer, actual preparation completion, cancellation and settlement. Caller loss and actor revocation cancel the original job. Publication and serialization share current-authority locks. The extension owns one actual Chrome promise chain, retained DOM guard and metadata-only settlement outbox; matching durable acknowledgment releases its exclusion lease. Unknown cleanup retains uncertainty. Only bounded exact-document partial excerpts are represented; this is not mailbox completeness evidence.
- The extension performs actual permission/document verification before final guard removal. Correlated `finishPageExcerpt` is the final awaited operation; synchronous current-authority checks and matching DOM revisions precede content publication. Empty content requires the same guard correlation. The new `read-job.ts` module separates actual job/outbox ownership from wire validation, a documented C4 scope refinement.
- Advisor reviewed the complete source diff and lifetime ordering. A second source-only reviewer found a settlement-envelope race: a worker could consume held revision-R content before the native loop applied the settlement's revision-R+1 withdrawal. Commit `e986d76` defers settlement dispatch until after common revision validation/invalidation, matching received-content ordering. Advisor and the independent reviewer both re-read the correction; the reviewer confirmed the finding resolved with no remaining issue in that focused review.

## Post-change verification

- At final source `e986d76`, Windows `pwsh -NoProfile -File scripts/verify.ps1 -Suite Static`: PASS, exit 0. Workspace format/Clippy with warnings denied and extension/Svelte checks passed; Svelte reported zero errors and warnings.
- At final source `e986d76`, Windows `pwsh -NoProfile -File scripts/verify.ps1 -Suite Build`: PASS, exit 0. Extension and desktop production assets built, then the full locked workspace release with `avesra-desktop/custom-protocol` completed in 7m11s. One Cargo job and BelowNormal process priority were used. The earlier `4aa1fa1` release attempt was intentionally interrupted for the review correction; the completed final run supersedes it. The isolated bundled executable was built, never launched or copied into the canonical primary checkout.
- Isolated artifact `E:\Dev\Avesra-plan-001-completion\target\release\avesra-desktop.exe`: 24,904,192 bytes, SHA-256 `6639BDF1861C5C9B814D4E814F8B1ACC0417FCEDDA25CED885D22E1056ECADFE`. This is build provenance only, not a second installed/canonical executable or runtime proof.
- Actual Spark2 immutable source `/home/medalink/.local/share/avesra-build/plan001-4aa1fa1`, same pinned compiler and resource limits as baseline: `bash scripts/verify.sh static` and `bash scripts/verify.sh build` both PASS, exit 0. Checking took 4.00 seconds and release compilation 32.79 seconds. Full output: `verification.log` in that directory. These commands cover contracts/core/server; desktop/extension code is outside this ARM gate. The final `e986d76` correction changes only desktop `browser.rs` and its specification; all ARM-covered source/build inputs are unchanged, verified from the two-file commit diff.
- No tests, fixtures, harnesses, live browser reads, microphone/playback, model requests, service changes or deployment ran. Windows builds stay in the isolated worktree; the primary canonical executable is not replaced.

## Verdict and continuation

**Plan 001 remains IN PROGRESS / PARTIAL.** C1 activation is blocked on the calibrated native qualification owner and measured operating points. C2 activation is blocked on concrete loaded-artifact identity, immutable routing, context-capacity and backend-terminal proof. C3/C5 require genuinely accepted tasks; C4 additionally needs a concrete borrowed-content consumer. OW1-OW3, A01-A29 and soak/performance evidence remain unverified. No saved enrollment, build result, generic page excerpt or successful dispatch may substitute for these requirements.

**Source checkpoint accepted after the review correction and passing applicable build/static gates.** This verdict applies only to the implemented slices and does not close the product plan. The advisor's index, plan and handoff updates accompany this review in a separate documentation commit. Source is isolated; no merge or push is authorized by this execution workflow.
