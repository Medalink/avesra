Current activity/TTS cancellation uses a shared worker event and a correlated
actual reset/CUDA-retirement terminal, preserving a warm model only after that
acknowledgement. Uncertain work still uses kill/reap. See
[the lifecycle contract](../../docs/audio-cancellation.md). Images built before
this source change retain their previous cancellation behavior.

# Spark audio lanes

The optional `activity.config.json` lane uses the existing NeMo/ASR image recipe
with a separate pinned Sortformer artifact and supervisor. It supplies bounded
four-speaker activity scores for explicitly requested VoiceCheck recordings; it
does not classify owner identity or grant voice readiness. See
[activity measurements](../../docs/voice-activity.md) for exact artifact, private
deployment configuration and the isolated 2026-09-25 model load/generated-audio
observation. Activity quality and controller/native integration remain unqualified.

Current generated-voice setup uses `voice-design.config.json` and `tts-voices.config.json` as separate lanes, with a shared absolute `/voices` mount owned by the service UID and mode0700. The controller needs both corresponding private deployment files; connection alone cannot create those services. See the2026-09-25 [operations record](../../docs/operations.md) for deployed image, actual generated-candidate evidence and remaining cold-start/readiness limits. This supersedes historical statements below that generated assets cannot be persisted or selected.

The Python 3.12 supervisor serves one separately configured lane per process over a private Unix socket. No LAN audio port, microphone capture, authorization, identity decision, ambient history, or raw-media persistence is implemented here. Deployed images use bounded whole-utterance inference and advertise `streaming: false`. New opt-in ASR and TTS streaming source is described below; no streaming path has been qualified. M3 remains incomplete.

Each socket directory must belong to the service UID and have mode 0700. The socket is owner-only and checks Linux `SO_PEERCRED`. Run the controller and container with the same UID. Messages are big-endian four-byte length plus UTF-8 JSON, at most 2,000,000 bytes; one request per connection, at most eight connections. One inference/load is admitted per lane with no waiting work queue. Raw input is PCM signed 16-bit little-endian mono 16kHz, base64 encoded, at most 30 seconds. The controller must preserve capture/session freshness and reject unknown speech before durable history or effects.

Protocol v1 has `health`, `load`, `infer`, and `cancel`. Health needs only integer `version: 1` and `operation`. Other requests additionally require canonical nonnil `request_id` and `session_id` UUIDs, positive integer `capture_epoch`, `sequence`, `issued_at_ms`, and `expires_at_ms`. Infer adds a `payload` object; all unknown fields fail closed. Cancel names the active request UUID in the same session with a fresh sequence. Lifetimes are at most 30 seconds, except model load (120 seconds). Admission converts remaining wall-clock lifetime once to a monotonic deadline. Absolute wall-clock drift over one second from the process monotonic anchor rejects admission, including forward jumps; restart after correcting the clock. Sequence/epoch tombstones remain for 125 seconds, longer than any request lifetime plus clock tolerance, with at most 16 retained sessions. Capacity exhaustion rejects explicitly.

Cancellation or expiry terminates the isolated child and releases its model allocations. The next load is cold. A worker still alive after kill/join remains tracked as termination_pending and blocks replacement until observed dead. Every child response is rechecked against generation, session epoch, and monotonic deadline before publication. The process is warm between successful calls, but input/result references are discarded after delivery. Parent and child never log request bodies. Child Python streams and OS output descriptors are redirected to the null device because third-party libraries may include sensitive content in errors. Errors crossing the boundary are fixed categories. A successful load is `loaded_unqualified`, never `ready`; capability and live owner quality remain separate evidence.

Speaker inference returns only a sensitive derived embedding or insufficient-duration result. It neither classifies owner/unknown nor grants permission. Quality, overlap, replay resistance, enrollment and thresholds remain controller work. TTS requires an explicitly selected generated WAV plus adjacent JSON declaring `kind: selected_generated_voice`, exact `base_revision`, and reference `text`. No request can choose a file path. Voice design returns a transient preview; persistence and selection are not implemented. Model changes must invalidate incompatible selected assets.

Run separate environments for `speaker`, `asr`, and `tts` extras; do not combine NeMo and Qwen dependency sets. `Dockerfile.speaker` derives the pinned existing ARM64 CUDA runtime and pins its full inspected package delta. Built image identity and qualification are separate evidence. All three runtime dependency deltas and final source images are pinned; the dated entries below distinguish artifact loads from current supervisor deployment. Model directories are mounted read-only, cache is private and distinct from Local Studio. Inference containers use no network and explicit CPU/memory limits. No tests or synthetic audio suites are created under the owner's instruction.

Primary API references: [SpeechBrain audio compatibility](https://speechbrain.readthedocs.io/en/stable/audioloading.html), [SpeechBrain releases](https://pypi.org/project/speechbrain/), [NeMo 3 runtime](https://docs.nvidia.com/nemo/speech/3.0.0/starthere/install.html), [Qwen voice API](https://github.com/QwenLM/Qwen3-TTS/blob/main/qwen_tts/inference/qwen3_tts_model.py). Upstream Qwen's non_streaming_mode flag does not create incremental audio output.

2026-09-24 observed qualification: pinned base imports Torch 2.13.0+cu130 and torchaudio 2.11.0+cu130. SpeechBrain 1.0.3 failed on removed torchaudio backend API, so the candidate uses 1.1.1. Import succeeded; actual ECAPA artifact loading succeeded on GB10 in an ephemeral no-network container with 2 CPUs and 6GiB memory limit. The adapter overrides the model YAML's remote pretrained path with the pinned local directory; without this override, offline loading correctly failed. No speech was supplied, embedding inference performed, enrollment created, or model left resident. This proves import/artifact load only. The final source/config image must be rebuilt and pinned before deployment.

Additional2026-09-24 qualification: separate NeMo3.0.0 and QwenTTS0.1.1 candidate images imported and loaded the existing ASR/Base artifacts on GB10, offline with2CPUs and8GiB limits. Both exited without speech input or inference. Qwen uses SDPA; optional SoX executable and flash-attn were absent. Package deltas were recorded against the immutable base and are now exact requirements files. Their final locked-layer/source images remain to be rebuilt; no streaming or capability success is inferred from loading.

2026-09-26 security update: `asr.requirements.txt` moves hydra-core 1.3.2 to 1.3.4 (PYSEC-2026-3850) and lightning 2.4.0 to 2.6.6 (PYSEC-2026-3624; matches the already pinned pytorch-lightning 2.6.6), installed with `--no-deps` despite NeMo 3.0.0's `<=` metadata bounds. Both versions were at least 7 days old. The ASR candidate image must be rebuilt and re-imported on GB10 before the earlier load evidence applies again. The TTS set keeps transformers 4.57.3 and accelerate 1.12.0 because QwenTTS 0.1.1, the newest release, pins them exactly; their advisories (fixes need transformers 5.x; none exists for accelerate) stay open until Qwen supports a fixed version.

Speaker service inspection used image `sha256:3bc76e6b0f2cfe6c37e219dde740ac69a70a959f6b4d87a3e3f8206d3163d59f`, built with exact speaker requirements and then-current source. Private no-network container `avesra-speaker-preflight` runs as UID1000 with2CPUs/6GiB; socket is `/home/medalink/.local/share/avesra-audio/run/speaker.sock`. Shipped administration client health showed unavailable, then load returned loaded_unqualified and health kept successful_inferences0/last_inference_msnull. This establishes the actual supervisor/child/socket load path only. Source subsequently gained graceful SIGTERM cleanup, so that source correction is not yet in the running image.

Operator CLI: `python -m avesra_audio.admin health --socket <private-socket>` reads only service metadata; `load` explicitly requests bounded model loading. Neither command accepts audio, text, profiles or prompts. Model-load errors return a failing exit status. Service SIGTERM cleanup terminates the child and removes its own socket; inspect stopped-container ownership before manually removing a stale socket from an older image.

Final pinned source31aaecd images built2026-09-24: speaker `sha256:65296b9328863c5c689b500d305648c944dd0111f4046a17ecd3290da2a433b0`, ASR `sha256:9256e738088d2531674c6ff02ec02e229cad056208ff4e136474d85a83be2188`, TTS `sha256:5c3d411b1825c673d8bd48089a4040c5a3ab7401029316d7ea475ae589cfcbfe`. The new ASR supervisor runs in `avesra-asr-preflight` with same-UID private `run/asr.sock`, network none,2CPUs/8GiB, all capabilities dropped and no-new-privileges. Shipped admin load and independent Rust health reported exact revision,loaded_unqualified,streamingfalse,authorityfalse,zero inferences and no last-inference duration. No speech was provided. The final speaker image was subsequently loaded in avesra-speaker-final-preflight and wired to the enrollment controller, as recorded in docs/operations.md. The old speaker preflight container was stopped while preserving its image/configuration for rollback. The final TTS supervisor was subsequently started on its separate private tts-final.sock and passed admin load plus compiled Rust health with zero inferences; see docs/operations.md for the deployment identity. No generated voice is selected and no synthesis or streaming has been qualified. Existing Local Studio/Comfy/Qwen services stayed active.

Incremental ASR source is now available behind the separate asr-streaming.config.json option. It uses the actual NeMo cache-aware API and bounded rolling features; existing deployed images/configuration remain batch and nonstreaming. See docs/streaming-asr.md for the private stream contract and unqualified status. Enabling the option requires rebuilding the complete supervisor/driver source image and qualifying its new capability with the controller. No speech or synthetic inference has been run for this source change.

Incremental TTS source is available behind the `tts_streaming: true` configuration option on a TTS lane with an explicitly selected generated voice preset. It emits early codec audio through a version/hash-bound adapter, with bounded child pipe backpressure and a final complete/truncated record. See docs/streaming-tts.md. No deployed image/configuration changed, no voice was generated or selected, and the controller/native playback bridge remains to be implemented.
