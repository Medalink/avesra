# Local audio drivers

ASR, speaker identity and TTS runtime compatibility remain unverified. This isolated Python 3.12 environment deliberately has no unqualified model dependencies or pretend-ready server. No model inference runs here yet.

Before enabling a lane, pin the ARM64/GB10-compatible runtime/dependencies and downloaded model revision, expose bounded cancellation/health/capability interfaces, and directly verify concurrent operation on Spark2. No test suite is introduced under the owner's instruction.
