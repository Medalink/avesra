# Actual owned reasoning recovery

The separate owned27B engine now passes the actual observer and a bounded greeting generation. This is operator evidence, not native voice acceptance or full Plan001 completion.

The adapter failure was a Python name collision: a nested `http` function hid the imported `http.client` module. The minimal `request_http` rename preserves all existing identity checks and production deadlines. Corrected `services/reasoning/engine.py` SHA256: `a6532cd4ba9129ca51ea6c18f3cee3a48ae540578d8dcbe5b1b7deea543825d0`.

The first cold operator greeting failed during logged Triton kernel compilation. Its pending record remains retained. Before another request, the isolated controller explicitly terminated only that owned instance; exact container retirement and disappearance of all four captured host PID/start-token identities were observed. The failed request was not relabeled successful.

The subsequent controlled load used:

- Container `a4a5c84563cd1657c4b403d982a4a360ca2ef74ebbe57883cc117e2c48124d24`.
- Image `sha256:d7c6827ade3234f66b365a492ff81e927ed64fc1c73f180aaefab69ebf8c2dd0`, unchanged36GiB memory/no-swap/2CPU limits and768MiB KV cache.
- Fresh capture SHA256 `471bd78668cf92dad1cbfd79abdab6133dcdc059f137daff2c6704664c92b987`.
- Actual engine identity `fee3367bd9aaffacbfdd28e0975f8b7bcc88407d187abc5cd162bcd3f2dfc938`.
- Inspection capacity4096; complete quality fingerprint `3e02e75e5100900944f9cdd9f5409d52d2e3378aae64e018e436c04088b74ffd`.

A distinct authorized setup warmup, bounded to120seconds and32output tokens, completed in44.952seconds with output `Ready`, correlated stop, DONE and actual EOF. A subsequent fresh greeting through the unchanged30second production helper completed in3.327seconds, using50prompt tokens and the normal512output-token ceiling. Its strict answer JSON contained: “Hello! I'm ready to talk. What's on your mind?” Its stream reached correlated stop and DONE after actual drain. No accepted native turn or action permission was created.

Local ignored evidence and operator scripts are retained under `artifacts/owned-reasoning-recovery-20260925/`, including `setup-warmup.json`, `operator-greeting.json` and `failed-greeting-retirement.json`. Current private Spark configuration and capture are adjacent files under `~/.local/share/avesra-build/owned-reasoning-warm-20260926/`. Install that directory's `reasoning.json` and `reasoning-artifact.json` together; do not reuse the prior capture beginning `5d467`. Credentials remain referenced through the separate private controller environment file and are not included in evidence.

## Future cold setup

Before exposing a newly loaded cold instance to ordinary requests, finish controlled artifact capture/load and actual inspection, then perform a distinct bounded setup warmup. The retained `warmup-owned.py` performs the observed120second/32token operation against only the exact owned container's loopback API. It preserves pending ownership on failure and checks correlated stream termination plus unchanged container incarnation on success. The retained `greeting-owned.py` then provides one explicit normal-budget operator observation. These are operator deployment steps, not automatic application retries or substitutes for native acceptance.

Never repeat an uncertain warmup or greeting merely because a deadline elapsed. Preserve its record and establish actual retirement; if necessary, terminate only the exact owned instance through its isolated controller, observe its container/process retirement, and perform a fresh controlled load. Do not increase production deadlines, restart unrelated models, or treat metrics showing zero requests as a correlated terminal. No automatic cold-warmup integration is claimed by this evidence.
