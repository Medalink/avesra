# Installed UI and typed-preview observations — 2026-09-26

This record covers the real installed Windows application at
`4da17b00224b79ad7cbe794439fcf2a73d7c21ef`. It records specific observed controls,
layout and preview output; it does not establish complete mock parity, beta
readiness, acoustic playback or owner-voice qualification. No tests, builds or
application sessions were run to write this document.

## Source and retained evidence

Evidence is retained locally under
`E:/Dev/Avesra/artifacts/ui-parity-4da17b0/`. `build-receipt.json` records Windows
Static, release build and package verification passing, store schema 30 and
telemetry schema 7. Its `deployment_performed: false` describes packaging before
the separately performed installed inspection below.

| Artifact | SHA-256 |
| --- | --- |
| `source.tar` | `cfc65dcecae486e01a567a5662cab0be96becb5f52cbb3a93277b1aec3edc651` |
| `package/avesra-desktop.exe` | `9e8b38718fb58096b9eb674fa71e34ae54a46df36f8540923aa1788c5554a28b` |
| `package/avesra-native-host.exe` | `2947ba0793daaae704501fc337cede66597b2cc60b50fe5328a7c6b822304d03` |
| `installed.wav` | `3e1b42f140a4badf7fc05d90f59fbebe7888e1ed8766c3f5c32b3c74f89093d5` |
| `typed-preview.mp4` | `c4d0cd27364fb1e22b8d7ff36c255355b056fd0f2b28eacd15b720efebfeb219` |

The controller remained at `695fc3e45323e3aeec62cf7e8ef1aa3e32c6167a`, with
binary SHA-256 `cf67565a0b8137b2ed2d4f20d4aaf5367d6c70c6ef434e0e32820e5999d6be18`;
the receipt records no controller source change. The owned diagnostic app was
PID 25924, with operator-recorded WebView PID 61172, on
`AvesraInspection-840314f802714448a9b7ce26a440bfa6`. The launch record identifies
the non-input desktop and unchanged `Default` input desktop. The operator closed
only that exact owned app after inspection. Physical output was intentionally
silent; the owner's foreground session and microphone were not used for proof.

## Observed controls and layout

- **Overlay:** the operator used actual Pause assistant, Resume and Collapse
  controls. Paused state showed “Listening and new actions are paused.” and an
  enabled Resume control. Expanded dimensions were 440 × 370 CSS pixels at DPR
  1.25; the restored compact window was 440 × 124. `overlay-settled-dom.txt`
  records opacity 1 for all six listed controls. The earlier expanded capture
  contains transition opacity below 1 and is not the settled visibility proof.
  The actual setup reason appeared once, with explicit unavailable task/history
  copy rather than invented activity. See `overlay-paused-dom.txt`,
  `overlay-restored-compact.json` and corresponding images.
- **Audio details:** `audio-initial-dom.txt` records the idle microphone meter
  at 24 CSS pixels high within the 880 × 640 Settings window. This is geometry
  proof, not microphone capture. `chimes-installed-dom.txt` and its image show
  the grouped learning/action card. A real learning-volume edit produced 14
  while action volume remained 15 and the footer reported unsaved changes.
  Discard restored both to 15 (`chime-draft.json`, `chime-discarded.json`).
- **Setup guide:** the inset guide rail measured 200 CSS pixels wide and each
  of its six rows measured 36 pixels high. Actual state labels included Spark
  Complete, speech services Review status, devices Selected, personal voice
  Unavailable, and optional voice/actions. This proves the inset guide only;
  standalone onboarding geometry and completion remain unproven.

All six setup destinations and the Browser shortcut reached their real Settings
heading and focused that heading, as retained in `setup-*-destination.json`:

| Guide destination | Observed heading/focus |
| --- | --- |
| Spark connection | Profiles & Machines |
| Speech services | Models & Drivers |
| Audio devices | Audio & Voice |
| Your voice | People & Voice ID |
| Avesra's voice | Audio & Voice |
| Actions & access | Observation & Actions |
| Browser shortcut | Profiles & Machines |

These route observations did not exercise Windows Hello, enroll a voice, inspect
private history or grant new permissions.

## Typed preview and separate earlier media

The real text control contained this 88-byte phrase:

> Avesra is reading my typed words with the selected voice, effects, and background music.

The operator used Play text with the existing selected reference. The native WAV
sidecar records 182,400 frames, 7.6 seconds, 24 kHz stereo, finalized on stream
disposal. Its capture provenance is native postmix/postformat before hardware
silencing; `hardware_silent` is true and `audible_device_proof` is false.

`typed-preview-frames/capture.json` retains 193 timestamped production Settings
frames from 16:46:02.370Z to 16:46:41.643Z. The MP4 is a 39.273-second sampled
WebView sequence rendered at 10 fps, with a requested original capture interval
of 200 ms and native audio offset 18.55307412147522 seconds. It is not a
continuous recording of the owner's desktop. Frame `00106.png`, captured at
16:46:23.938Z, visibly shows the ruby audio bars, the full phrase in the input,
its shortened caption and actual synthesis/playback busy state. After completion,
`typed-preview-observed-dom.txt` records `canvas: []`, restored Play text and
“Final preview samples submitted to the output device.” That empty canvas is a
post-completion observation, not evidence that the live bars were missing.

The earlier default-preview media retain their separate identity under
`E:/Dev/Avesra/artifacts/ui-parity-1eab298/`, built from
`1eab298b77e95bf92bfcc24efe45d12bae6003ce`. They are not this typed-text run:

| Earlier artifact | Identity and duration |
| --- | --- |
| `installed.wav` | SHA-256 `af2210ae8e012ee0cf863a7d7f42b96f3f8e139cc961eafacef7823aa0998a07`; 9.35 s, 24 kHz stereo, hardware silent |
| `default-preview.mp4` | SHA-256 `00bf30989d80963fb1e8c5c5eb8d2f1d518000d8bb331afefa58cb556276a9e1`; 39.571 s, audio offset 16.187255859375 s |

Both captures establish app/mixer preview evidence only. Neither establishes
physical acoustic playback, speech intelligibility in the room, owner acceptance,
voice qualification or automatic action correctness.

## Recovery, preservation and remaining limits

After Pause/Resume, the initial voice status refresh reported “Wait for the Spark
session” and Play text was unavailable. A later real manual Refresh restored the
existing selected voice and enabled Play text before the recorded preview. This
recovery is part of the observation; uninterrupted readiness was not proved.

The diagnostic run also retained a stale paused reason in the Personal voice
status after Resume. Source review attributed the diagnostic capture worker's
one-time return to this limitation; normal startup still owns the ongoing
producer. This record does not treat that explanation as a demonstrated normal
startup recovery or conceal the stale diagnostic status.

`preserved-final.json` records the original Razer Seiren X microphone and default
Realtek SPDIF output, 125% scale/DPR 1.25, pace 1.0, voice volume 100, presence and
background 85/85, learning/action levels 15/15 and no unsaved changes. The
120-byte default greeting was restored. No claim here covers populated private
panels, owner voice capture, failure-path qualification or complete mock parity.

## Future manual repeat

1. Verify the installed source/binary identity and retain a new evidence folder.
   Use the documented non-input diagnostic desktop method; leave the input
   desktop, owner microphone and physical playback untouched. Record baseline
   selected devices, scale and audio levels before interaction.
2. Open the real Overlay, wait for transitions to settle, inspect visible
   controls and bounds, then use Pause assistant, Resume and Collapse. Record
   actual statuses, failures and restored geometry without substituting state.
3. In Audio & Voice, inspect the idle meter and grouped chimes. Change only the
   learning draft by one step, verify the other level stays unchanged, then
   Discard and confirm both original values.
4. Open the setup guide and follow each of the six real destinations and Browser
   shortcut. Confirm its actual heading/focus and reported availability; do not
   start Hello, enrollment or permissions merely to obtain a completed badge.
5. If the existing selected reference is available, type the phrase in the real
   control and use Play text. Record any required Refresh or failure. Retain
   timestamped app frames and native silent-output sidecars; inspect live bars,
   exact input text and completion separately. Do not inject private IPC or
   substitute a simulated preview.
6. Restore the default text, all changed controls and original selections. Verify
   a clean footer and baseline values, then close only the identified owned
   diagnostic process. Keep acoustic and owner-qualification claims separate.
