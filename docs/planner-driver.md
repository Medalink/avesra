# Paired actor registration and accepted planner/reply transport

This slice follows the durable native conversation and exact-app producer. It cannot activate voice: there is still no qualified runtime profile or current-profile producer. The normal reasoning/TTS entry point must remain separate from generated-voice setup preview. A model, browser page, extension, frontend flag or received transcript cannot create a native accepted turn.

## Explicit native actor registration

The paired companion is the trusted native authority for its OS-backed owner setup, subject to the existing same-user local-code threat boundary. Spark does not independently verify Windows Hello. The native registration command must consume the original one-use protected Settings proof before its first await, read the durable owner/current OS principal, and bind the exact paired device, acknowledged control session/generation and immutable owner actor. The Settings action must describe that it registers this local owner with the paired Spark. Do not send the Windows SID, enrollment vectors or raw authentication evidence over this route.

A separate strict versioned HTTPS registration request carries a native-generated request UUID, actor UUID, current control session/action epoch and original expected registration revision. It is authenticated with the current paired-device credential and admitted only while the control session is unlocked, unpaused and action-enabled. Mute/deafen are separate from action permission. The server binds one immutable owner actor registration to that device in its transactional authentication metadata. A repeated exact read can reconcile an uncertain write, but a different actor or unknown/malformed existing row must not replace it. Explicit revocation is revision-bound and invalidates outstanding planner/reply authority. Neither registration nor an owner label creates an application/browser grant. Device revocation always invalidates its actor binding at admission and during in-flight work.

Native owner configuration is an assertion from the authenticated companion, not proof derived from a model response or an arbitrary remote actor ID. Only the protected native adapter may issue registration; no extension/page forwarding or general webview HTTP proxy exists. Final publication rechecks the original native proof/context after blocking reads and at commit initiation. Lost replies are reconciled by exact request/device/actor/revision; there is no automatic replacement registration or silent rollback.

## Accepted request ownership

An opaque stored native planner claim is the source of a reasoning request. The existing actual ledger worker must transition one accepted turn into one unique planner request transactionally, retain bounded accepted text and original actor/device/session/action provenance, and reject cancelled, suspended, already-linked or answered records. Exact app commands may finish through the prior native resolver; they need no model. The request deadline is fixed at native admission and must include queue/transit/driver time, with no renewed lifetime after retries. Restart suspends uncertain planning; it never reissues it automatically.

The public controller route checks paired-device authentication, exact active actor-registration revision, current acknowledged session/action epoch and a source-turn/request replay record. The native request is a privileged assertion of durable acceptance by that companion; `accepted: true` is neither required nor sufficient. The server-generated private model request ID is distinct from all client IDs. Bound request body, pending count, per-device retained replay entries, global actual driver jobs, sends and idle waits. Reject resource exhaustion rather than evicting an ID while a response could still exist. A missing/expired control heartbeat, Stop/pause/lock/disconnect, actor/device revocation or action-epoch change cancels publication and the owned request. Capture-only mute must not revoke an already accepted request.

Cancellation retains actual ownership until the HTTP/model operation finishes or the configured backend confirms cancellation. A dropped network future alone must not free a second model slot or claim that GPU work stopped. If the Local Studio backend lacks request cancellation proof, record cancellation pending/unavailable and retain the bounded actual job until its completion/deadline semantics are known; do not reset unrelated serving engines.

## Local Studio reasoning adapter

Use the existing Local Studio deployment owner and authenticated proxy. A private Avesra configuration selects an exact recipe/deployment, model ID/artifact revision, endpoint and credential file; no request/model output chooses a URL, credential, model path or serving engine. Keep the endpoint loopback or explicitly certificate-bound to the configured Spark. Disable redirects and environment proxy inheritance. The adapter may read deployment metadata and send an explicitly admitted accepted request; it must never start, swap, evict or modify Local Studio automatically to satisfy it.

Before readiness, validate the installed API's actual status/model identity and supported text/structured-result capability. Downloaded artifacts, an HTTP 200, a configuration label or historical synthetic probe is not current readiness. Implement a bounded typed adapter against the inspected API shape; leave unconfigured/incompatible states explicit. No floating dependency changes, model loads, inference probes or evaluation runners are part of this source-only phase.

The planner sees the accepted user text and only separately authorized context. It returns a bounded untrusted typed answer, NeedsInput question, or proposed operations. No planner field is an authority ID, grant, approval, executable command or observed postcondition. Unknown/extra fields, malformed result, nonterminal/truncated completion and mismatched deployment/request IDs are rejected. For the initial answer path, no tools are executed and no browser/app facts are fabricated; action proposals remain NeedsInput until a native exact resolver or concrete owner clarification freezes their scope. General planner prose cannot establish that an effect succeeded.

## Durable reply and normal TTS

The native worker accepts a correlated result only for the actual current stored planner claim and action context, validates its exact source and fixed deadline, then stores an immutable reply revision under that accepted turn. No response received after cancellation may create a replacement turn/reply. A question with no tools remains a legitimate conversation; it is not converted into an empty AcceptedIntent. Task outcome speech must instead reference native observed finalization, preserving UnknownEffect explicitly.

Only an opaque stored reply handle may enter normal TTS. The public synthesis stream binds registered actor, accepted turn/reply/request revision, current paired session, independent playback epoch and exact selected generated-voice identity including audio and metadata digests. The native companion must recheck reply state and current output authority before opening the device lease. No arbitrary setup-preview text endpoint is added. Deafen/Stop/pause/lock/disconnect/output device or voice changes withdraw output; mic mute alone preserves it.

Keep private streaming TTS ownership and fixed synthesis deadline, native startup/prebuffer headroom, paced packet age, sequence/utterance replay checks, final complete/truncated distinction and submitted-versus-heard drain semantics. The preview transport primitives may be reused internally only after this separate accepted-reply admission. Nothing in this contract asserts a live actor registration, planner readiness, reply, generated voice or audible output.

## Inspected Local Studio transport evidence

Reviewer read-only source inspection on 2026-09-25 used the installed `/home/medalink/local-studio` revision `d1abdef` with its existing dirty patches preserved. No HTTP, model or lifecycle operation was invoked. `controller/src/modules/proxy/passthrough-routes.ts` exposes POST `/v1/chat/completions`, resolves configured recipe served-model identity and returns `model_not_running`/503 instead of automatically launching it. `/v1/models` includes installed recipes with an `active` property, so membership alone is not readiness. Provider/model routing may include cloud destinations: require an explicit configured local recipe, never a slash/provider route or caller-selected model.

The proxy forwards streaming data, includes periodic keepalive comments, can emit a data error object after an HTTP 200, and may end without the terminal `[DONE]` marker on failure. A streaming adapter must independently require its complete typed terminal/result; HTTP success or EOF cannot establish a complete answer. The proxy combines client and request abort signals for upstream fetch. This is HTTP abort propagation only and does not prove engine/GPU cancellation. Credential configuration is referenced through `controller.env`; no credential value is part of this evidence or this repository.

Until backend cancellation is qualified, cancelled publication may retain the actual upstream reader/slot to observe normal completion while dropping content. A deadline, transport loss or truncated EOF that leaves backend execution uncertain must withdraw adapter availability and retain a durable outstanding-request uncertainty record, rather than allowing restart or a new caller to overlap a possibly live old request. Recovery requires explicit observed backend request termination or a verified deployment-generation replacement; a relabelled configuration, elapsed local timer or Avesra process restart is insufficient. This lifecycle boundary must be implemented before enabling reasoning admission.

## Actor registration wire and persistence contract

The current transport is strict actor protocol 2 on `/actors`: original request UUID, fresh transport-attempt UUID, control session UUID, positive action epoch, and one tagged command. Register carries the native owner's actor UUID and immutable local-owner revision; it is create-only for the authenticated paired device. Status is read-only. Revoke carries the exact actor and registration revision. Responses echo request/session/action context and return either no binding or the actual device/actor/local-owner revision/server registration revision/original registration request/revoked state. All UUIDs are non-nil. Unknown fields and omitted required fields fail; the route cannot register a grant or accept a transcript.

Authentication metadata advances transactionally from schema 1 to schema 2, adding one owner binding per device, with unique registration revision and original request ID. Older databases must have no pre-existing new actor table; schema 2 must match its exact supported SQL/indexes and reject attached triggers. Preserve existing device/pairing data. An occupied unversioned store is rejected using literal internal-name prefix filtering across all object types. Register does not overwrite or revive an existing binding. Revoke is exact-revision idempotent and keeps the immutable tombstone. A new paired-device identity may explicitly register the same durable local actor; actor UUID itself is not globally unique across devices.

The server must verify device activity and current action-session ownership again immediately before commit initiation, under its bounded actual writer ownership. Status/revoke may inspect an existing revoked owner binding, but a revoked paired device cannot use the route. In-flight planner admission must also consult binding revocation; registration state alone is not reasoning readiness. This first storage checkpoint is dormant until the protected native command and current action-session route are integrated.

## Source checkpoint: actor protocol and controller route

Actor protocol 1 and `/actors` now exist in source with the protected-native caller still pending. Authentication schema 2 preflights the exact actor table/index mapping and attached-object count before migration; fresh creation and prior schema-1 upgrade are transactional. Occupied unversioned databases check every user object with a literal SQLite prefix. Register is create-only; status returns real binding or absence; revoke preserves the exact immutable record and original registration identity. Response validation binds the original command, device, request and action-session values. No grant, transcript, model, owner SID or qualification is carried.

The controller retains two bounded actor-writer slots through actual blocking auth-store work. Existing header authentication retains its separate bounded actual-work permit if its waiter times out. The actor route uses a fixed five-second monotonic budget, explicit action-session permission (initially disabled until Mode), heartbeat freshness and a withdrawal flag. Control action changes cancel publication independently of capture/output-only changes; a cancelled HTTP waiter cannot release the actual storage job's slot. The callback rechecks device activity/current session before commit initiation. A late/uncertain commit can only be reconciled by status; returning an error is not a rollback claim. Native protected registration, actor-aware planner lifecycle and normal reply/TTS remain unwired and unavailable. The route has not been invoked.

Additional reviewer inspection of the unchanged installed Local Studio source confirmed `Authorization: Bearer` or `x-api-key` authentication, constant-time comparison to configured `api_key`, and `LOCAL_STUDIO_API_KEY` environment loading. The relevant inspected proxy/model/auth files are clean against d1abdef; unrelated patches remain preserved. No secret configuration file/value or HTTP endpoint was read for this evidence.

## Native registration continuation

Add explicit Register owner with Spark, Refresh registration and exact Revoke registration actions inside the existing approved Owner card (Settings.dc.html552–590), using its p-3.5/gap-3 geometry and small native buttons. Keep the existing Windows verification banner; registration/revocation are protected owner management, status is read-only. Actual remote binding, unavailable/uninspected state and pending local intent must be shown distinctly from voice readiness. Hiding Settings invalidates management work and frontend publication; no background auto-registration or retry is introduced.

Before sending Register, the native actual owner coordinator must persist a create-only local registration intent in a small versioned SQLite store. It binds paired device, durable local actor/owner revision and original native request UUID. No credential or SID is stored there. Reuse this exact intent only after another explicit authorized Register action; an uncertain network result cannot mint a new registration UUID. A matching remote Status response reconciles original device/actor/owner revision/registered-by identity. A different existing binding or corrupted local record stays unavailable for registration, with exact authenticated remote revocation still possible where the local owner identity matches. No automatic adoption, clear, replacement or rollback occurs.

The local intent store is bounded to 32 device records, rejects unsupported/malformed schemas and unexpected attached objects before writes, and shares the existing native owner-management admission rather than opening a second live effect ledger. The actual storage/HTTP coordinator retains that admission through completion even if the invoking webview future is dropped. Original Settings proof and current paired owner/action-session context are checked after each preparation await, before intent commit initiation, before network send and before publication. Settings close/cancel must propagate correlated operation cancellation to the server; it must not advance the global action epoch and cancel unrelated accepted tasks. Dropping HTTP alone is insufficient. A post-commit invalidation may suppress display but never claims that a remote write was undone.

The acknowledged native SessionIdentity adds its actual server-echoed action epoch, keeping capture and playback fields independent. Registration admission requires the current action epoch to be acknowledged, not merely copied from local settings. Status refresh may read a server-committed binding after a lost write; late responses may not repopulate a replacement connection or hidden/unmounted card. Gain, media devices, profile qualification and accepted-task activation are unaffected by this source work.

Actor protocol 2 distinguishes the durable original registration request UUID from a fresh per-transport-attempt UUID. Correlated cancellation addresses only that attempt and its original authenticated device/session/action context; an older cancel cannot stop a later explicit retry that reuses the durable registration intent. Keep a bounded in-flight registry under the actual writer lifetime and bounded retired attempt IDs per control session, rejecting exhaustion/reuse rather than evicting live authority. Cancellation admission must not share the two actor-write slots, and it never creates/replaces registration state.

Closing Settings immediately withdraws local publication and sends best-effort authenticated remote cancellation. Networks and commit initiation create an unavoidable distributed boundary: cancellation arriving after commit starts cannot undo or conclusively report the write as absent. Exact status reconciliation is required before another explicit decision. Persisting the original registration identity before send remains mandatory; a new transport attempt never means a new durable registration identity.

## Source checkpoint: attempt cancellation and acknowledged action context

Protocol 2 requires the fresh attempt UUID in request/reply and adds strict `/actors/cancel` with exact attempt/session/action context. Its version is independent from the paired control protocol and auth-storage schema. The live control session retains at most 128 attempt IDs (including cancellation-before-operation tombstones) without eviction; exhaustion requires an explicit new control session. Every operation retires its admitted ID, and every callback/final publication checks the original uncancelled attempt. A late cancellation cannot mutate registration state or affect a different attempt. Cancellation has two separate bounded slots and a constant-time session-scoped credential-digest check established only by successful control authentication. It does not wait for AuthStore or an actor writer. This withdrawal-only proof cannot register, read, revoke a binding or create another session; active device/database authentication remains mandatory for authority operations. Cancellation may still race commit initiation, so exact status remains necessary after uncertainty.

The companion's SessionIdentity now includes only the validated server-echoed action epoch. The existing voice current-context check also requires that acknowledged action epoch to equal the local and originally admitted action epoch. Native registration will consume the same acknowledgement instead of optimistic local values. Protected registration commands, persisted local intent and Owner-card actions are still the next slice; no request, cancel or voice capture was invoked.

The initial protocol-2 draft reused AuthStore authentication for cancellation, which could wait behind the very transaction being cancelled. Source review corrected this before native integration: only the already-authenticated live control session stores the bounded non-serialized withdrawal digest; `/actors/cancel` holds only its separate admission and the session registry mutex. The writer callback takes auth then session; cancellation never takes auth, and no session-to-auth inversion is introduced. Session closure removes the proof. A revoked credential may at most withdraw its still-closing original session's work; it cannot regain authority through this path.

Local registration intents also bind the verified server-certificate SHA-256 fingerprint, so a coincident device UUID on a different configured Spark cannot reuse the original intent. Changing URL spelling alone does not rewrite that certificate/device binding. The native helper derives this fingerprint from the already-validated pairing certificate, never from frontend input. Corrupt intent rows or a changed owner revision remain explicit recovery cases; status/revoke can still inspect the exact remote binding without automatically repairing local history.

The native intent-store source now provides bounded read-only recovery and create-only remember operations under an exact version-1 SQLite schema. Both reads and writes use a two-second busy timeout. Its record stores only canonical certificate fingerprint plus device/actor/owner revision/original request; it contains no credential, SID, transcript or profile. Interrupted uninitialized empty files remain recoverable, occupied unversioned stores reject, and row count is capped before insertion. This store is not invoked until the following native coordinator is wired; its original-authority callback does not itself establish Windows verification.


## Native protected actor setup implementation

The companion now exposes Settings-only registration status, protected Register and exact-revision Revoke commands. One actual owner-management coordinator retains the shared owner slot across native principal/owner reads, DPAPI pairing reads, certificate-derived fingerprinting, local intent commit and TLS response/cancellation. Its fixed 15-second monotonic admission starts before window visibility inspection and includes preparation and the seven-second HTTP request; blocked native reads/writes remain owned even if they outlive admission. Cancellation has a separate maximum three-second withdrawal allowance. No retries or replacement request identities are generated automatically.

Original one-use management proof is consumed before the first await. A challenge snapshot precedes the synchronous window getter; no window getter runs under Runtime.local. Every preparation completion and publication rechecks the original challenge, unlocked/current native connection and actual acknowledged action session. Local intent publication also rechecks the exact native owner revision before commit initiation. A dropped IPC waiter withdraws its retained coordinator; Settings hide, lock, setup cancellation and session changes withdraw through the original challenge/context. After send, errors or withdrawal trigger one correlated remote cancellation while the actual owner slot remains held. A commit already initiated remains uncertain, never rolled back by the reply.

The actor-specific HTTPS client disables environment proxies, redirects and built-in trust roots, trusts only the validated configured certificate, and bounds requests/replies to 1024/2048 bytes. Strict actor-v2 validation checks original request, fresh attempt, paired device, acknowledged session/action and command-specific binding. Status continues to the server when local intent metadata is corrupt, distinguishing remote absence, exact original-intent registration, different owner, revoked record and unreconciled binding. Revoke is constrained to the native local actor and exact provided registration revision. No repaired/adopted intent or readiness is inferred.

The existing approved Owner card contains explicit small-button rows and actual registration state with the existing verification banner. The card starts uninspected, invalidates late publication on native hide/section disposal/context change, and retains busy ownership until each invoking promise ends. Status is explicitly refreshed; no periodic registration traffic occurs. Existing People section disposal cancels setup natively. Source checks do not invoke these commands, create an intent, register/revoke an actor, invoke Windows authentication or qualify a planner/voice pipeline.


The acknowledged native control context additionally binds the actual authenticated control record's paired device UUID and DER-certificate SHA-256 bytes. Actor preparation compares its loaded DPAPI pairing against both before remembering an intent or sending a request. This closes the interval where a newly saved pairing may exist before its replacement connection advances the generation; an old acknowledged session cannot be combined with the new pairing. No credential is copied into SessionIdentity or exposed to the UI.


## Next slice: durable native planner claim and reply

The ledger advances to schema 7 for two exact checked tables: one immutable planner request per accepted turn and one immutable reply per planner request/turn. Migration rejects pre-existing lookalike tables, unexpected attached objects and missing uniqueness before mutation. Required source tuples are re-read and cross-checked against the accepted record; text remains bounded to 8192 UTF-8 bytes. Restart suspends pending claims, never reconstructs an opaque dispatch handle or automatically resends text.

A native-only request consumes the existing opaque DurableTurn (including a NeedsInput result from the exact-app resolver), exact registered actor binding and frozen source DispatchSession. Claim admission begins one fixed 30-second monotonic budget; queue time, transaction work, transport and result persistence all consume it. The same actual NativeEffects owner claims and finalizes, with no second Store connection. Claim fails for cancelled/suspended/answered turns, task-linked turns, any prior planner claim, wrong source/actor/capture/action provenance, revoked or different registration, or expired admission. It stores a native request UUID plus the exact registration revision and source before exposing any text for transport. The caller's native callback must revalidate actual acknowledged session, device/certificate context, current action permission and original registered actor. Capture-only mute does not invalidate the frozen accepted capture epoch.

The claim handle has private fields and no Deserialize or Clone. Its bounded transport view is an assertion from the authenticated native companion, not a token the model may generate. The wire carries version 1, native request/turn/revision, actor and registration revision, original utterance/frozen capture epoch, live original session/action context, accepted text and only the remaining monotonic budget. There is no `accepted` boolean, executable target, browser context, grant creation, model choice or arbitrary URL. A controller must independently authenticate and bind these fields before a model request; merely adding this contract does not enable the route.

The initial strict result allows only Answer or NeedsInput with bounded plain text, exact original request/turn/registration/session/action correlation and explicit complete terminal evidence from the configured adapter. There are no model-proposed tool payloads in this first path. Rejection, malformed response, expiry, cancellation or incomplete backend terminal cannot be stored as an answer. The actual worker checks its persisted immutable claim against the owned handle, revalidates current native action authority and deadline, then atomically inserts a new immutable reply revision and transitions the accepted turn to answered or waiting_input. A reply is an assistant response, not a verified external fact or effect observation.

Only successful finalization produces an opaque stored reply handle. Status/history return bounded metadata without recreating request/reply authority; explicit cancellation remains available through the original accepted source. Normal TTS integration must consume the opaque reply and independently check current output/voice selection before playback. A stored or imported row alone cannot bypass native acceptance, model lifecycle uncertainty or playback admission. The qualified voice producer, controller reasoning driver and normal reply transport are still pending source integrations; no model/runtime action is authorized by this spec checkpoint.
